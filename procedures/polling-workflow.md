# Polling Workflow

**Triggers:** "start polling", "poll", "begin polling"

The polling loop is now embedded in each lifecycle procedure:
- **Dev roles:** `procedures/dev-lifecycle.md` (Phase 0 + Loop Control)
- **QA roles:** `procedures/qa-lifecycle.md` (Phase 0 + Loop Control)

Read the lifecycle document for your role function and execute its full loop.
Quick-start prompts: `ai_team_config/prompts/claude/{role}-polling.md`
